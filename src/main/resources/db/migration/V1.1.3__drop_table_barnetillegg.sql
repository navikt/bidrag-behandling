-- TABLE: BARNETILLEGG

/* ##### RULLE TILBAKE ######

    CREATE TABLE IF NOT EXISTS PUBLIC.BARNETILLEGG
    (
        ID BIGINT NOT NULL GENERATED BY DEFAULT AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 9223372036854775807 CACHE 1 ),
        BEHANDLING_ID BIGINT NOT NULL,
        IDENT TEXT COLLATE PG_CATALOG."DEFAULT",
        DATO_FOM DATE,
        DATO_TOM DATE,
        BARNETILLEGG NUMERIC NOT NULL DEFAULT 0,
        CONSTRAINT BARNETILLEGG_PKEY PRIMARY KEY (ID),
        CONSTRAINT FK_BEHANDLING_ID FOREIGN KEY (BEHANDLING_ID)
            REFERENCES PUBLIC.BEHANDLING (ID) MATCH SIMPLE
            ON UPDATE NO ACTION
            ON DELETE NO ACTION
    )

    TABLESPACE PG_DEFAULT;

    ALTER TABLE IF EXISTS PUBLIC.BARNETILLEGG
        OWNER TO "BIDRAG-BEHANDLING-MAIN";

    GRANT ALL ON TABLE PUBLIC.BARNETILLEGG TO "BIDRAG-BEHANDLING-MAIN";

    GRANT ALL ON TABLE PUBLIC.BARNETILLEGG TO CLOUDSQLIAMUSER;

    DELETE FROM FLYWAY_SCHEMA_HISTORY WHERE VERSION = '1.1.3';

 ########*/

DROP TABLE BARNETILLEGG;








